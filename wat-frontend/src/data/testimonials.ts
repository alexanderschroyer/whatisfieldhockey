export type Testimonial = {
	text: string;
	attribution: string;
};

/**
 * Real quotes only, with permission. Empty array shows an invitation to share — no placeholder quotes.
 */
export const testimonials: Testimonial[] = [];
